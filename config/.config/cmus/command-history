delete
sdasd
bind -f common . seek ]
bind -f common . seek +1m]
bind common u shell ~/.config/cmus/update.sh
add ~/Music
clear
player-prev
player-next
next
bind -f common u shell ~/.config/cmus/update.sh
clear 
bind -f common / search-start
shuffle
set status_display_program=/home/sublime/.config/cmus/notify
set status_display_program=~/.config/cmus/notify
:set status_display_program
:set status_display_program=~/.config/cmus/notify
bind -f common , vol -10%
bind -f common , vol +10%
bind -f common . vol +10%
bind -f common , vol -1% -0
bind -f common ] vol +0 +1%
bind -f common , vol +0 +1%
bind -f common . vol +1% +0
set shuffle=false
set show_current_bitrate=true
set replaygain=disabled
set lib_sort=artist album title filename
bind -f common { vol -1% -0
bind -f common ^D win-half-page-down
q
Q
reload
colorscheme sublime
